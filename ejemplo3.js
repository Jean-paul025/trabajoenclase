let ocupacionCancha = true;
let nombreCancha = "Bernabeu";

let mensaje = ocupacionCancha ? `la cancha llamada ${nombreCancha} esta ocupada` : `la cancha lamada ${nombreCancha} esta disponible`;

console.log(mensaje);